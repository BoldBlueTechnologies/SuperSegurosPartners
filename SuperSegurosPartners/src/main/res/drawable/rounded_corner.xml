<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color de fondo -->
    <solid android:color="@color/white1_superapi" />

    <!-- Bordes redondeados -->
    <corners android:radius="8dp" />

    <!-- Borde (opcional) -->
    <stroke android:color="@color/white1_superapi" android:width="1dp" />
</shape>